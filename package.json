{
  "name": "case-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-env.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "docx": "^9.6.0"
  }
}
